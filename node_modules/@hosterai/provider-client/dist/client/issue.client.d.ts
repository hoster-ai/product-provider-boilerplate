import { Axios } from "axios";
import { IssueDto } from "../dtos/issue.dto";
import { ICLient } from "./client.interface";
export declare class IssueClient implements ICLient<IssueDto> {
    private axios;
    private api_url;
    private service_provider_token;
    constructor(axios: Axios, api_url: string, service_provider_token: string);
    create(data: IssueDto): Promise<any>;
    update(title: string, data: IssueDto): Promise<any>;
    delete(title: string): Promise<any>;
    get(title?: string): Promise<any>;
    private request;
}
