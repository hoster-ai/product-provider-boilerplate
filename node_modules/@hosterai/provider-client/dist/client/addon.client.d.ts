import { Axios } from "axios";
import { AddonDto } from "../dtos/addon.dto";
import { ICLient } from "./client.interface";
export declare class AddonClient implements ICLient<AddonDto> {
    private axios;
    private api_url;
    private service_provider_token;
    constructor(axios: Axios, api_url: string, service_provider_token: string);
    create(addon: AddonDto): Promise<any>;
    update(name: string, addon: AddonDto): Promise<any>;
    delete(name: string): Promise<any>;
    get(name: string): Promise<any>;
    private request;
}
