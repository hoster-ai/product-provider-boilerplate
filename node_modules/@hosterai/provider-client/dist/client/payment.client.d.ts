import { Axios } from "axios";
import { PaymentDto } from "../dtos/payment.dto";
import { ICLient } from "./client.interface";
export declare class PaymentClient implements ICLient<PaymentDto> {
    private axios;
    private api_url;
    private service_provider_token;
    constructor(axios: Axios, api_url: string, service_provider_token: string);
    create(data: PaymentDto): Promise<unknown>;
    update(name: string, data: PaymentDto): Promise<unknown>;
    subscribe(data: PaymentDto): Promise<unknown>;
    unsubscribe(data: PaymentDto): Promise<unknown>;
    delete(data: string): Promise<unknown>;
    get(data: string): Promise<unknown>;
    success(data: PaymentDto): Promise<unknown>;
    cancel(data: PaymentDto): Promise<unknown>;
    failed(data: PaymentDto): Promise<unknown>;
    private request;
}
