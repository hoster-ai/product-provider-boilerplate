import { Axios } from "axios";
import { ProductDto } from "../dtos/product.dto";
import { ICLient } from "./client.interface";
export declare class ProductClient implements ICLient<ProductDto> {
    private axios;
    private api_url;
    private service_provider_token;
    constructor(axios: Axios, api_url: string, service_provider_token: string);
    create(data: ProductDto): Promise<unknown>;
    update(name: string, data: ProductDto): Promise<unknown>;
    delete(data: string): Promise<unknown>;
    get(data: string): Promise<unknown>;
    private request;
}
