import { Axios } from "axios";
import { TemplateDto } from "../dtos/template.dto";
import { ICLient } from "./client.interface";
export declare class TemplateClient implements ICLient<TemplateDto> {
    private axios;
    private api_url;
    private service_provider_token;
    constructor(axios: Axios, api_url: string, service_provider_token: string);
    create(data: TemplateDto): Promise<any>;
    update(name: string, data: TemplateDto): Promise<any>;
    delete(id: string): Promise<any>;
    get(data?: string): Promise<any>;
    private request;
}
