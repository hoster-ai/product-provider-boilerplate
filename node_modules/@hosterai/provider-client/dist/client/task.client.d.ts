import { Axios } from "axios";
import { TaskDto } from "../dtos/task.dto";
import { ICLient } from "./client.interface";
export declare class TaskClient implements ICLient<unknown> {
    private axios;
    private api_url;
    private service_provider_token;
    constructor(axios: Axios, api_url: string, service_provider_token: string);
    create(data: TaskDto): Promise<any>;
    update(id: string, status: "in-progress" | "cancel" | "complete"): Promise<any>;
    delete(id: string): Promise<any>;
    get(id?: string): Promise<any>;
    complete(id: string, data: unknown): Promise<any>;
    inProgress(id: string, data: unknown): Promise<any>;
    setCompletePercentage(id: string, percentage: number): Promise<any>;
    cancel(id: string, data: unknown): Promise<any>;
    private request;
}
